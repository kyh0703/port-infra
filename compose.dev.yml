services:
  api:
    image: port-api-dev-runtime:local
    build:
      context: ../api
      target: deps
    command: ["sh", "-lc", "pnpm install --frozen-lockfile && exec pnpm dev"]
    environment:
      NODE_ENV: development
      CI: "true"
      CHOKIDAR_USEPOLLING: "true"
    volumes:
      - ../api:/app
      - api_node_modules:/app/node_modules

  web:
    image: port-web-dev-runtime:local
    build:
      context: ../web
      target: deps
    command: ["sh", "-lc", "pnpm install --frozen-lockfile && exec pnpm dev"]
    environment:
      NODE_ENV: development
      CI: "true"
      HOSTNAME: 0.0.0.0
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    volumes:
      - ../web:/app
      - web_node_modules:/app/node_modules

volumes:
  api_node_modules:
  web_node_modules:
